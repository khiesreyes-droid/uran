export type Device = {
  id: string;        // user-supplied free-text device identifier e.g. "living-room-sensor"
  name: string;
  address: string;
  latitude: number;
  longitude: number;
  createdAt: string;
};
